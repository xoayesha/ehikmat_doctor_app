name: ehikmat_doctor_app
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  # REST client
  http: ^1.2.1

  # Environment variables
  flutter_dotenv: ^5.1.0

  # UI helpers
  image_picker: ^1.1.0
  multi_select_flutter: ^4.1.3
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
